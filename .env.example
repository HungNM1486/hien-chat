# Server
PORT=4000
NODE_ENV=development
DATABASE_URL=postgresql://hiennha:hiennha@localhost:5432/hiennha
REDIS_URL=redis://localhost:6379
JWT_SECRET=change-me-in-production-use-long-random-string
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d
CORS_ORIGIN=http://localhost:3000

# MinIO
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=hiennha
MINIO_SECRET_KEY=hiennhasecret
MINIO_BUCKET=hiennha-media
MINIO_USE_SSL=false

# Web (public)
NEXT_PUBLIC_API_URL=http://localhost:4000

# WebRTC TURN (optional, for calls behind strict NAT)
# NEXT_PUBLIC_TURN_URL=turn:your-turn-server:3478
# NEXT_PUBLIC_TURN_USERNAME=
# NEXT_PUBLIC_TURN_CREDENTIAL=
